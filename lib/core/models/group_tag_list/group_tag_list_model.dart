class GroupTagList {
  String tagText;
  String subText;

  GroupTagList({required this.tagText, required this.subText});
}

tagTileDatas() {
  List<GroupTagList> tagTileDetails = [];

  GroupTagList tileData = GroupTagList(tagText: "", subText: "");

  //1
  tileData =
      GroupTagList(tagText: "#BookClub", subText: "13 Joined\n12/25 Spot Left");
  tagTileDetails.add(tileData);
  //2
  tileData =
      GroupTagList(tagText: "#BookClub", subText: "13 Joined\n12/25 Spot Left");
  tagTileDetails.add(tileData);
  //3
  tileData =
      GroupTagList(tagText: "#BookClub", subText: "13 Joined\n12/25 Spot Left");
  tagTileDetails.add(tileData);
  //4
  tileData =
      GroupTagList(tagText: "#BookClub", subText: "13 Joined\n12/25 Spot Left");
  tagTileDetails.add(tileData);
  //5
  tileData =
      GroupTagList(tagText: "#BookClub", subText: "13 Joined\n12/25 Spot Left");
  tagTileDetails.add(tileData);
  //6
  tileData =
      GroupTagList(tagText: "#BookClub", subText: "13 Joined\n12/25 Spot Left");
  tagTileDetails.add(tileData);
  //7
  tileData =
      GroupTagList(tagText: "#BookClub", subText: "13 Joined\n12/25 Spot Left");
  tagTileDetails.add(tileData);
  //8
  tileData =
      GroupTagList(tagText: "#BookClub", subText: "13 Joined\n12/25 Spot Left");
  tagTileDetails.add(tileData);
  //9
  tileData =
      GroupTagList(tagText: "#BookClub", subText: "13 Joined\n12/25 Spot Left");
  tagTileDetails.add(tileData);
  //10
  tileData =
      GroupTagList(tagText: "#BookClub", subText: "13 Joined\n12/25 Spot Left");
  tagTileDetails.add(tileData);

  return tagTileDetails;
}
